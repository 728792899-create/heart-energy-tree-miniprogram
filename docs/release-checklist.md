# Release Checklist

## 当前线上与发布候选

- 产品：心动能量树私人版 V3
- 当前线上：`3.0.0`，已于 2026-07-23 13:09:40 正式发布
- 候选小程序版本：`3.1.0`
- 云端兼容 buildTag：`heart-tree-private-v2-20260717-release-final-v1`
- 候选 releaseTag：`heart-tree-private-v3-20260723-unbind-consent-v1`
- 平台状态：`3.1.0` 尚未部署、上传、提审或发布
- 发布原则：先部署向后兼容云函数并核对 buildTag + releaseTag，再上传 `3.1.0`；旧 `3.0.0` 在切换期间必须继续可用

## 代码与公开基线

- [ ] 全新克隆、无 private config、无 `motion-studio/out` 时 `npm test` 全绿且测试数不少于 239。
- [ ] `npm run check:shared`、`check:syntax`、`check:cloud-deps`、`check:budgets`、`check:docs` 通过。
- [ ] Remotion compositions 和独立 still smoke 通过；`node_modules/`、`.cache/`、`out/` 未进入 Git。
- [ ] GitHub Actions 三个 job 在无微信账号/无私有配置环境通过。
- [ ] buildTag 与 releaseTag 在客户端、云函数、README 一致；旧 `3.0.0` 对新云函数保持兼容。

## 安全与数据

- [ ] 可信 OPENID、角色鉴权、事务、幂等、内容安全与共享同步测试通过。
- [ ] 双方解除流程验证不同确认者、两次警告、待办拦截、写入锁、收尾重试、邀请作废与旧关系冻结。
- [ ] 最终解除前先撤销双方信笺 inbox/state 的 `recipientOpenid`；失败不得伪装成解除成功。
- [ ] 客户端对所有业务集合无写权限；实时消息只读规则限定 `auth.openid`。
- [ ] 集合、索引、规则、备份、恢复和版本迁移按文档核对。
- [ ] 发布前备份可解析、计数与校验值已记录；回滚版本兼容 state version 4。
- [ ] 仓库与发布证据不含私人数据或秘密。

## 体验与内容

- [ ] 小字号/浅色文字、88rpx 触控、窄屏、键盘、弱网、加载、重试和空状态通过。
- [ ] 远程 MP4→poster→原生三级降级和每级失败路径通过。
- [ ] 简化动效与声音关闭都持久化；系统静音被遵守。
- [ ] 合法文字/图片不误拦，风险回调闭环和幂等证据齐全。
- [ ] ImageGen/设计源/Remotion 素材都在预算内，无水印、无私人照片、无版权不明新增物。

## 产品边界

- [ ] 仍为固定两人私人版，没有公共注册、租户选择或陌生人匹配。
- [ ] 心愿金/奖励只做内部记账和线下手动兑现；不接真实支付，没有微信支付、充值、提现 API 或金融收益文案。
- [ ] 关系解除明确为双方确认且不自动删除；导出、永久删除、照片清理、授权撤回和异常账号恢复仍按人工边界说明。

## 外部验收

- [ ] 微信开发者工具编译记录完成。
- [ ] 获授权后云函数部署、真实 buildTag 核对完成。
- [ ] 获授权后在隔离测试云环境完成双账号解除真机表；不得为验收解除现有线上私人关系。
- [ ] 未完成项明确标注“外部权限阻塞”，附责任人和下一步；没有用 web-demo/本地测试冒充。
